---
homelab:
  hosts:
    server:
      ansible_host: 192.168.1.100
      ansible_user: ansible
      ansible_become: yes
  vars:
    docker_version: "24.0"
    docker_compose_version: "2.28"
    timezone: "UTC"
    homelab_domain: "homelab.local"
    backup_retention_days: 30
